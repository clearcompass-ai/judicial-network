// Package integration holds the SDK-math-×-deployed-stack scenarios: submit over
// the API, then assert the outcome across the API response + Postgres (internal/db)
// + the Ledger's proofs. Build-tagged e2e; run against a live stack. Shared stack
// + assert helpers come from tests/harness.
package integration
