// Package wire holds the black-box, per-component WIRE-SHAPE contract scenarios
// (the SCENARIOS.md phase 0–8 catalog, L5–L11). Build-tagged e2e; run against a
// live stack. Shared stack + assert helpers come from tests/harness.
package ledger
